/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['en', 'ar'], // English and Arabic
      defaultLocale: 'en',
    },
    basePath: '/yaseer',
  };
  
  export default nextConfig;
  